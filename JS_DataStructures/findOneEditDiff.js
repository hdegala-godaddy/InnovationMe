
function findOnEditDiff(str1, str2){

    let largeStr = str1.length > str2.length ? str1 : str2;
    let smallStr = largeStr === str1 ? str2 : str1;

    if (largeStr.length === smallStr.length) {
        
    } else {
        
    }


}

